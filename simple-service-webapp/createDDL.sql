CREATE TABLE ADMIN (ID  SERIAL NOT NULL, PASSWORD VARCHAR(255), USERNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE LOCATION (ID  SERIAL NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PROPERTIES (ID  SERIAL NOT NULL, NAME VARCHAR(255), VALUE VARCHAR(255), PROPERTYGROUPS_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE LOCATIONPROPERTIES (ID  SERIAL NOT NULL, VALUE VARCHAR(255), LOCATION_ID BIGINT, PROPERTIES_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE PROPERTYGROUPS (ID  SERIAL NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE PROPERTIES ADD CONSTRAINT FK_PROPERTIES_PROPERTYGROUPS_ID FOREIGN KEY (PROPERTYGROUPS_ID) REFERENCES PROPERTYGROUPS (ID)
ALTER TABLE LOCATIONPROPERTIES ADD CONSTRAINT FK_LOCATIONPROPERTIES_LOCATION_ID FOREIGN KEY (LOCATION_ID) REFERENCES LOCATION (ID)
ALTER TABLE LOCATIONPROPERTIES ADD CONSTRAINT FK_LOCATIONPROPERTIES_PROPERTIES_ID FOREIGN KEY (PROPERTIES_ID) REFERENCES PROPERTIES (ID)
